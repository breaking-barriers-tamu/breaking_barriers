<!DOCTYPE html>
<html>
<head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
    <style>
        /* Style the notice paragraph with green color */
        p {
          font-size: 16px;
          color: green;
        }

        /* Style the main heading */
        h1 {

            font-family: Arial;
            font-size: 36px;
            color: #333;
            margin: 20px 10px;
        }

        /* Style the event logs container */
        #event_logs {
            background-color: #f2f2f2;
            padding: 20px;
            border-radius: 5px;
        }
        
        a {
            font-family: Arial;
            font-size: 30px;
            text-decoration: none;
            color: #007acc;
            font-weight: bold;
            margin: 20px 10px;
        }
    </style>
</head>
<body>
    <p><%= notice %></p>

    <h1>All Event Logs</h1>  
    <%= link_to 'Back to Admin Dashboard', admin_root_path %>

    <div id="event_logs">
        <% @event_logs.each do |event_log| %>
            <div>
              <%= render event_log %>
            </div>
        <% end %>
    </div>
</body>
</html>
