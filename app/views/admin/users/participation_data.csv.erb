<%- headers = ['First Name', 'Last Name', 'Email', 'Phone', 'Year',  'Total Volunteer Hours'] -%>
<%= CSV.generate_line headers %>
<%- @user_hours.each do |pair| -%>
  <%= CSV.generate_line([pair.first.first_name, pair.first.last_name, pair.first.email, pair.first.phone_number, pair.first.year, pair.second]) -%>
<%- end -%>