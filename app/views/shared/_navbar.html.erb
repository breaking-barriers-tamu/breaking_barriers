<%= stylesheet_link_tag "navbar" %>

<div class="nav-container">
    <div class="nav-shade">
        <div class="logo"> 
            <%= link_to "Breaking Barriers", root_path%>
        </div>
        <ul class="nav-items">
            <% if user_signed_in? %>
            <li><%= link_to "Events", events_path %></li>
            <% end %>
            <li><%= link_to "Annoucements", announcements_path %></li>

            <li><%= link_to "About", about_path %></li>
            <li><%= link_to "Contact", contact_path %></li>
            <% if current_user&.can_access_admin_dashboard? %>
                <li><%= link_to "Admin", admin_root_path %></li>
             <% end %>

            <% if user_signed_in? %>
            <%= button_to "Logout", destroy_user_session_path, data: {turbo: "false"}, method: :delete, class: "logout-button" %>
            <% else %>
            <%= link_to "Login", new_user_session_path %>
            <% end %>
        </ul>
    </div>
</div>
